# HIS (Healty Infromation System)

## Run API
```bash
cd backend && go run cmd/api/main.go
```

## Run Worker
```bash
cd backend && go run cmd/worker/main.go
```

## Run Frontend DEV
```bash
cd frontend && npm install && npm run dev
```